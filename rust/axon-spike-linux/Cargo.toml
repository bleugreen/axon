[package]
name = "axon-spike-linux"
version = "0.1.0"
edition = "2024"
publish = false

[target.'cfg(target_os = "linux")'.dependencies]
atspi = { version = "0.30.0", features = ["connection", "proxies", "tokio"] }
tokio = { version = "1.47.1", features = ["macros", "rt-multi-thread", "time"] }