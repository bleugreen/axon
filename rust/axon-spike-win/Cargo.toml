[package]
name = "axon-spike-win"
version = "0.1.0"
edition = "2024"
publish = false

[target.'cfg(windows)'.dependencies]
uiautomation = { version = "0.25.0", default-features = false, features = ["pattern"] }
